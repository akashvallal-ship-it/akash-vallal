# D:\Akash\src\agents\config.py

class Config:
    # Example configuration settings
    GITHUB_TOKEN = ""  # If your agent needs to access private repos
    DOCKER_IMAGE = "myagentimage"  # Name of your built Docker image (if custom)
    TIMEOUT = 600  # Default task timeout in seconds

    # Add more agent, repo, or test-related settings as needed

# You can expand this class with more attributes as your agent's needs grow.
